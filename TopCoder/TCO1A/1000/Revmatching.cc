#include <bits/stdc++.h>

#define fori(n) for(int i = 0; i < (int) (n); i++)
#define forj(n) for(int j = 0; j < (int) (n); j++)

using namespace std;

class Revmatching 
{
public:
    int smallest(vector <string> A) 
    {
        return int;
    }
};
